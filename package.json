{
  "name": "first-italian-pizza-pos",
  "version": "1.0.0",
  "description": "First Italian Pizza POS System",
  "private": true,
  "scripts": {
    "install:all":    "npm install --prefix backend && npm install --prefix frontend && npm install --prefix electron",
    "db:setup":       "cd backend && npx prisma migrate dev --name init && node src/utils/seed.js",
    "db:seed":        "cd backend && node src/utils/seed.js",
    "dev:backend":    "npm run dev --prefix backend",
    "dev:frontend":   "npm run dev --prefix frontend",
    "dev":            "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "build:frontend": "npm run build --prefix frontend",
    "build:exe":      "npm run build:frontend && npm run build --prefix electron",
    "start:backend":  "npm run start --prefix backend",
    "setup":          "npm run install:all && npm run db:setup"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
